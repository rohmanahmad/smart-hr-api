/api/v1/administration/company/create:
  post:
    tags:
      - Companies
    security: []
    summary: Create Company
    description: Create New Company in All Companies
    requestBody:
      content:
        application/x-www-form-urlencoded:
          schema:
            type: object
            properties:
              name:
                $ref: '#/definitions/generals/string'
              description:
                $ref: '#/definitions/generals/string'
              address:
                $ref: '#/definitions/generals/string'
              phoneNumber1:
                $ref: '#/definitions/generals/number'
              phoneNumber2:
                $ref: '#/definitions/generals/number'
              email:
                $ref: '#/definitions/generals/string'
              website:
                $ref: '#/definitions/generals/date'

    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  $ref: '#/responses/codes/200'
                message:
                  $ref: '#/responses/messages/success'
                data:
                  type: object
                  properties:
                    item:
                      type: object
                      properties:
                        code:
                          $ref: '#/definitions/generals/string'
      '4xx':
        description: Bad Request (Input Data Maybe Invalid)
        content:
          application/json:
            schema:
              $ref: '#/responses/4xx'
      '500':
        description: Error From Internal
        content:
          application/json:
            schema:
              $ref: '#/responses/5xx'
