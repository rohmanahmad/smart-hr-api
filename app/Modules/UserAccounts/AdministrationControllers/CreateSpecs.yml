/api/v1/administration/user-accounts/create:
  post:
    tags:
      - User Accounts
    security: []
    summary: Create User Account
    description: Create New User Account in All Companies
    requestBody:
      content:
        application/x-www-form-urlencoded:
          schema:
            type: object
            properties:
              companyCode:
                $ref: '#/definitions/generals/string'
              email:
                $ref: '#/definitions/generals/string'
              username:
                $ref: '#/definitions/generals/string'
              password:
                $ref: '#/definitions/generals/password'
              confirmPassword:
                $ref: '#/definitions/generals/password'
    responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  $ref: '#/responses/codes/200'
                message:
                  $ref: '#/responses/messages/success'
                data:
                  type: object
                  properties:
                    item:
                      type: object
                      properties:
                        code:
                          $ref: '#/definitions/generals/string'
      '4xx':
        description: Bad Request (Input Data Maybe Invalid)
        content:
          application/json:
            schema:
              $ref: '#/responses/4xx'
      '500':
        description: Error From Internal
        content:
          application/json:
            schema:
              $ref: '#/responses/5xx'
